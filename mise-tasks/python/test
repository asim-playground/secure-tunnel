#!/usr/bin/env bash
#MISE description="Run Python tests"
set -euo pipefail

mise run python:build
cd "${MISE_PROJECT_ROOT}/python"
uv run pytest tests
SECURE_TUNNEL_PYTHON_SKIP_BUILD=1 mise run python:smoke-package
