#!/usr/bin/env bash
#MISE description="Run Rust tests with nextest"
set -euo pipefail

source "${MISE_PROJECT_ROOT}/mise-tasks/rust/python-runtime-env"

cargo nextest run --workspace --all-features --no-fail-fast
