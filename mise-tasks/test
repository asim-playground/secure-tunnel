#!/usr/bin/env bash
#MISE description="Run the default test suite"
set -euo pipefail

mise run rust:test
mise run rust:test-doc

mise run python:test
SECURE_TUNNEL_PYTHON_SKIP_BUILD=1 mise run sdk:python-fastapi-smoke

mise run go:test
